use crate::instruction::Instruction;

#[allow(unused, unreachable_code)]
pub fn decode(data: &[u8], address: usize) -> Instruction {
    todo!()
    /*
    let (inc, instr) = Instruction::parse(slice);
    head += inc;
    digest.push(instr);
    */
}
